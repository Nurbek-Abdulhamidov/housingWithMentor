import React from "react";
import { Button, Input } from "../Generic";

const Home = () => {
  return (
    <div>
      Home
      <Input placeholder="Name" width="150" />
      <Button>Click</Button>
    </div>
  );
};

export default Home;
